import React from 'react'; import { useNavigate } from 'react-router-dom'; import { session } from '../lib/storage';
export default function DashboardPage(){const s=session();const n=useNavigate();return <main className='page'><section className='card'><h1>Dashboard</h1><p>Link: {location.origin+s?.link}</p><button onClick={()=>navigator.clipboard.writeText(location.origin+(s?.link||''))}>Copy Link</button><button onClick={()=>n('/feed')}>Go Feed</button><button onClick={()=>n('/settings')}>Settings</button>{s?.role==='admin'&&<button onClick={()=>n('/admin')}>Admin Panel</button>}</section></main>}
