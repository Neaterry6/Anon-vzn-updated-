import React,{useState} from 'react';import { session, setSession } from '../lib/storage';
export default function SettingsPage(){const s=session();const [push,setPush]=useState(true);const [email,setEmail]=useState(false);return <main className='page'><section className='card'><h1>Settings</h1><p>{s?.username} {s?.igHandle}</p><button onClick={()=>navigator.clipboard.writeText(location.origin+(s?.link||''))}>Share My Link</button><label><input type='checkbox' checked={push} onChange={e=>setPush(e.target.checked)}/>Push Notifications</label><label><input type='checkbox' checked={email} onChange={e=>setEmail(e.target.checked)}/>Email Notifications</label><button onClick={()=>{setSession(null);location.href='#/login'}}>Log Out</button></section></main>}
